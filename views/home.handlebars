<div>
	<h1>Lista de productos</h1>
	{{#if products}}
		{{#each products}}
			<h2>{{this.title}}</h2>
			<p>{{this.description}}</p>
			<p>{{this.code}}</p>
			<p>{{this.price}}</p>
			<p>{{this.stock}}</p>
			<p>{{this.category}}</p>
			{{#if this.thumbnails}}
				<p>{{this.thumbnails}}</p>
			{{/if}}
		{{/each}}
	{{else}}
		<h2>No se agrego ningun producto</h2>
	{{/if}}
</div>